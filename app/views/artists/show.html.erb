<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @artist.name %>
  <br>
<!-- for a single artist display all of their songs -->
	<ul>
  <% @artist.songs.each do |s| %>
  <li><%= s.name %></li>
  <%end%>
  </ul>
</p>


<%= link_to 'Edit', edit_artist_path(@artist) %> |
<%= link_to 'Back', artists_path %>
